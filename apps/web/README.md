# Web application boundary

Status: planned; no runnable web application exists in this directory.

The proposed Next.js/React/TypeScript client implements the [UI specification](../../docs/UI_SPECIFICATION.md). Begin with the research activation journey in the [delivery plan](../../docs/DELIVERY_PLAN.md). Add the pnpm workspace, shared UI package and generated API contracts when implementation begins. See [architecture](../../docs/ARCHITECTURE.md).
