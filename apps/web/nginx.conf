server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;

    # SvelteKit's static adapter emits /login as login.html (trailingSlash: never).
    location / {
        try_files $uri $uri.html $uri/index.html =404;
    }

    error_page 404 /404.html;
}
