# @maester/financial-engine (TypeScript, placeholder)

This package will hold the TypeScript extraction pipeline once it reproduces the behaviour of the Python reference. Until then, the Python package at `packages/financial-engine` is the frozen reference implementation.
